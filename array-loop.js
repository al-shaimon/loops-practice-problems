// Target: Display every elements of an array

var numbers = [45, 48, 30, 32, 245, 1234, 32, 21, 54];
for(var i = 0; i < numbers.length; i++){
  var number = numbers[i];
  console.log(number);
}

// Target: Display every string of an array

var items = ['udvash', 'laptop', 'qbank', 'mobile', 'cube'];
for(var i = 0; i < items.length; i++){
  var item = items[i];
  console.log(item);
}